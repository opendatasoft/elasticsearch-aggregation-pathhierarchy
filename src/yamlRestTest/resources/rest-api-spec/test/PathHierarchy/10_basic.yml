"PathHierarchy Aggregation plugin installed":
  - do:
      cluster.state: {}

  - set: {master_node: master}

  - do:
      nodes.info: {}

  - match: {nodes.$master.plugins.0.name: pathhierarchy-aggregation}

  - do:
      info: {}

  - match: { cluster_name: "yamlRestTest" }
